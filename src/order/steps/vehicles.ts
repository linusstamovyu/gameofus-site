// PLACEHOLDER written by the scaffold; the vehicles agent replaces this file.
import { el } from "../../dom";
import { heading, type StepView } from "../context";

export const vehiclesStep: StepView = (_ctx, panel) => {
  panel.append(heading("Coming soon", "Getting around"), el("p", null, "This step is being built."));
};
